# Менеджер Локальной Базы Знаний (RAG v3.1)

Мощный консольный инструмент для создания и управления **локальной базой знаний** (RAG - Retrieval Augmented Generation).  
Скрипт превращает ваши документы в векторы (эмбеддинги) и сохраняет их в ChromaDB, используя локальный сервер LM Studio.

> **Полная приватность:** Все работает офлайн. Никакие данные не отправляются в облако.

---

## ✨ Ключевые возможности (v3.1)

*   **🎯 Выборочная индексация:** Вы сами решаете, какие файлы обрабатывать. Можно выбрать один файл, диапазон (например, `1-5`) или всё сразу.
*   **🧠 Умная нарезка (Smart Chunking):** Текст разбивается рекурсивно (по абзацам, предложениям), сохраня смысловой контекст.
*   **⚡ Инкрементальное обновление:** Скрипт запоминает MD5-хэши файлов. При повторном запуске он **пропускает** неизмененные файлы.
*   **🚀 Пакетная обработка (Batching):** Отправляет данные в LM Studio пачками (по 50+ штук), ускоряя процесс в 10-20 раз.
*   **🗂 Управление коллекциями:** Создание, переключение, очистка и полное удаление коллекций прямо из меню.

---

## 📦 Требования

| Компонент | Версия / Примечание |
|-----------|---------------------|
| **Python** | 3.10+ |
| **LM Studio** | Любая версия с поддержкой Local Server |
| **ChromaDB** | `chromadb` (последняя версия) |

### Установка зависимостей

```bash
# Создание окружения (Windows)
python -m venv venv
.\venv\Scripts\activate

# Установка библиотек
pip install chromadb openai pypdf python-docx tqdm colorama
```

---

## ⚙️ Настройка

Скрипт ищет настройки в классе `Config` (в начале файла) или в переменных окружения:

| Переменная | Описание | Значение по умолчанию      |
|------------|----------|----------------------------|
| `CHROMA_DB_PATH` | Путь к базе данных | `С:\chroma_db`             |
| `DEFAULT_DOCS_DIR` | Папка с документами | `С:\Documents`             |
| `LM_STUDIO_URL` | Адрес сервера | `http://localhost:1234/v1` |

---

## 🚀 Использование

### Шаг 1: Подготовка LM Studio
1. Откройте **LM Studio** -> вкладка **Local Server**.
2. Загрузите модель эмбеддингов (например, `nomic-embed-text-v1.5` или `all-MiniLM-L6-v2`).
   * *Внимание: Для качественного RAG лучше использовать Nomic (размерность 768).*
3. Нажмите **Start Server**.

### Шаг 2: Запуск
```bash
python rag_manager.py
```

### Шаг 3: Работа с меню

#### 1️⃣ Индексация папки (С выбором файлов)
Скрипт просканирует папку, отсортирует файлы по алфавиту и покажет список с номерами:

```text
Найдено файлов: 15
Примеры списка:
  [1] Android_Architecture.pdf
  [2] Backend_API.md
  ...
  [15] ZeroToHero_Guide.txt

Как индексировать?
  [Enter] или 'a' -> Все файлы
  '1-5'           -> Диапазон (с 1 по 5 включительно)
  '10+'           -> С 10-го до конца
  '5'             -> Только файл №5
```

**Примеры ввода:**
*   `a` — Обработать всю папку (пропустив те, что не менялись).
*   `1-3` — Обработать только первые 3 файла.
*   `5` — Переиндексировать конкретный файл (например, если вы его только что поправили).

#### 2️⃣ Управление коллекциями
Позволяет разделять знания по темам (проектам).
*   **(N)ew:** Создать новую коллекцию.
*   **(S)witch:** Переключиться на другую.
*   **(D)elete:** Удалить текущую коллекцию **насовсем**.

#### 3️⃣ Очистка (Truncate)
Удаляет векторы внутри коллекции, но оставляет её имя. Полезно, если вы сменили модель эмбеддинга и хотите пересчитать всё заново.

---

## 🛠 Устранение неполадок

### 🔴 Ошибка: `Collection expecting embedding with dimension of 768, got 384`
**Причина:** Вы создали коллекцию, используя одну модель (например, `nomic` с размерностью 768), а теперь пытаетесь искать или добавлять данные другой моделью (например, `all-MiniLM` с размерностью 384).
**Решение:**
1.  Либо загрузите в LM Studio ту же модель, которой создавали базу.
2.  Либо **очистите коллекцию** (Пункт 3 в меню), чтобы перезаписать данные с новой моделью.

### 🔴 Нет связи с LM Studio
**Решение:** Убедитесь, что сервер запущен на `localhost:1234`.

### 🔴 Файл не читается
**Решение:** Скрипт поддерживает `.txt, .md, .pdf, .docx, .py, .json`. Если файл битый или зашифрован, он будет пропущен с пометкой `[READ ERROR]`.

---

## 🔌 Интеграция с MCP (Cursor / Claude)

Чтобы подключить эту базу к IDE:

1.  В конфигурации `chroma-mcp` укажите тот же путь, что и `CHROMA_DB_PATH`.
2.  Убедитесь, что модель эмбеддинга в LM Studio совпадает с той, которой вы индексировали файлы.
3.  Имя коллекции по умолчанию: `main_collection`.

Приятной работы! 🚀